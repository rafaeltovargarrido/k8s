apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "curso-chart.fullname" . }}-test-connection"
  labels:
    {{- include "curso-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "curso-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
